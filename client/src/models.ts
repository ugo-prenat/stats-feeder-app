export type Streamer = {
  bot: string;
  token: string;
  twitchId: Number;
  name: string;
  username: string;
  profileImageUrl: string;
  email: string;
  isAuth: boolean;
}
export type Lang = 'fr' | 'en';